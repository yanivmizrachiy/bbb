# תוכנית עבודה — שרטוטים ומערכות צירים בתקן ספר-לימוד (פרויקט-כולו)

> מסמך עבודה חי. מטרה: **כל** שרטוט בפרויקט — ברור, מדויק, חזק להדפסה, ואחיד.
> התקן המחייב מוגדר ב-`STYLE_GUIDE.md` (סעיף "מערכות צירים"); כאן — המיפוי, מה בוצע ומה הלאה.

## 1) מיפוי הטכנולוגיות (מצב שנמצא)
| טכנולוגיה | היכן | מצב לפני | מצב אחרי |
|---|---|---|---|
| `coord()` | algebra8 (t01/t03/t04/t07) | מספרים נחתכים על הציר כש-xmin=0; ללא הילה; שוליים קבועים | ✅ קנוני: שוליים דינמיים, הילה, שנתות, צירים 2.2 |
| `numline()` | algebra8 (ציר מספרים) | תקין בסיסי | ✅ קנוני, מסונכרן לכל הנושאים |
| `points_plot()` | algebra (ז') | תקין (מספרים מחוץ לגרף) | ✅ צירים/מספרים חוזקו בסריקה הרוחבית |
| `vbar`/`line_chart` | כל הנושאים | צירים חיוורים, בלי שנתות | ✅ כהים + שנתות + מספרים כהים |
| `geo.coord_grid()` | geometry8 (ציור מעגל) | בלי מספרים ושנתות | ✅ מספרים+שנתות+צירים 2.2 |
| SVG ידני (`_rise_fall_svg`) | algebra8 t01 | צבעי צירים ישנים קשיחים | ✅ עודכן לדיו כהה |

## 2) העיקרון המבני (כדי שהבעיה לא תחזור)
- **מימוש קנוני אחד** ל-`coord`/`numline`/`grid_of`/`_clip_line`/`_POS`, זהה בייט-בייט
  בכל ארבעת `charts.py`. עריכה — רק ב-algebra8, ואז סנכרון לשאר.
- **אכיפה אוטומטית**: `tools/check_uniformity.py` נכשל (`[AXES]`) אם מימוש חסר או סטה.
  רץ לפני כל commit — סטייה עתידית נתפסת מיד, לא בדף מודפס.
- **דף חדש** משתמש אך ורק ברכיבי המנוע — אסור SVG ידני לצירים.

## 3) בוצע (10.7.2026)
- [x] כיול-עומק של `coord()`: שוליים דינמיים לפי רוחב המספרים, הילת-לבן לכל תווית,
      מספרים `#1f2a44` משקל 600, צירים 2.2, שנתות, שורת-תוויות מרווחת.
- [x] סנכרון הערכה הקנונית לכל ארבעת הנושאים + שומר `[AXES]`.
- [x] `geo.coord_grid` באותו תקן; תיקון ה-SVG הידני.
- [x] סריקה רוחבית קודמת: צירים כהים + שנתות ב-`vbar`/`line_chart`; דיו חזק לכיתובים.
- [x] כל שאלת "סרטטו" מקבלת צירים מוכנים מכוילים לנתונים.

## 4) הצעדים הבאים (לפי הצורך, בסדר עדיפות)
1. **ביקורת עמוד-עמוד של איורי `img()`** (סריקות מקור): חדות/ניגודיות להדפסה;
   שחזור וקטורי לגרפים שנקבעים מתמטית (עפ"י `STYLE_GUIDE.md` §גרפיקה).
2. **איחוד `points_plot` אל `coord`** (אלגברה ז') — מיזוג הדרגתי כשנוגעים בדפים אלה.
3. **בדיקת קריאות בהדפסה אמיתית** (מדפסת לייזר ש/ל 300dpi) לעמודים מדגמיים מכל נושא.
4. **הרחבת השומר**: השוואת `vbar`/`line_chart` בין הקבצים (כיום מסונכרנים ידנית).

## 5) איך מוסיפים דף עבודה חדש (צ'ק-ליסט)
1. תוכן ב-`topics/*.py`; צירים — רק `C.coord(...)` / `C.numline(...)` מכוילים לנתונים.
2. `python <subject>/build.py` → בדיקת העמודים שנוצרו ויזואלית.
3. `python tools/check_uniformity.py --heights` — חייב לעבור נקי.
4. commit + push (Vercel מתעדכן לבד).
