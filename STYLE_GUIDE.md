# מדריך עיצוב — "מתמטיקה לחטיבת הביניים"

המסמך הזה הוא **מקור האמת היחיד** לעיצוב ולסימון המתמטי בכל עמודי הפרויקט.
כל קובץ/נושא חדש **חייב** לעמוד בו, וכל שינוי עיצובי מתבצע כאן קודם. הרץ
`python tools/check_uniformity.py` לפני כל קומיט כדי לוודא אחידות.

> כלל-על: המנוע הקנוני הוא **`wsengine.py`** (משותף ל-`algebra8/` ו-`geometry8/`).
> כל נושא מודולרי חדש מ-ש-ת-מ-ש ב-`wsengine.py` כמות-שהוא — לא מעתיקים CSS ידנית.

## גופנים
- גוף הטקסט והכותרות: `'Segoe UI','Arial',sans-serif` בלבד.
- SVG (גרפים/שרטוטים): אותו stack דרך `FONT` ב-`charts.py`/`geo.py`.
- גודל בסיס: `11pt`, `line-height:1.55`, צבע דיו `#1f2a44`.

## צבעים
- דיו: `#1f2a44` · משני: `#5b6573` · רשת/מסגרת: `#e6e8ee` / `#c7ccda`.
- גרדיאנט שער (זהה בכל הנושאים): `linear-gradient(135deg,#4f46e5,#7c3aed 55%,#0d9488)`.
- פלטת צבעי-מקטע (`--c` שמועבר ל-`SECTION`): `#4f46e5` `#0d9488` `#7c3aed` `#e11d48` `#d97706` `#0284c7` `#059669` `#2563eb`. בוחרים מתוכה בלבד.

## גיאומטריית עמוד ושבירה
- `@page { size:A4; margin:16mm 12mm 18mm 12mm; }` → גובה תוכן ≈ **263 מ"מ**.
- **כל כרטיס שאלה (`.q`) חייב להיות ≤ ~260 מ"מ** כדי ש-`page-break-inside:avoid`
  ישמור עליו שלם ואף סעיף לא "יברח" לעמוד חדש. אם חורגים — מקטינים איור (`fig(w=)`/`img(w=)`)
  או מצמצמים שורות תשובה. בדיקה: `tools/check_uniformity.py` או מדידה ב-Playwright (print).

## רכיבים (ערכים מחייבים)
- `.q`: `border:1px solid #e6e8ee; border-radius:12px; padding:12px 16px 14px; margin:11px 0`.
- `.qnum`: עיגול `--c`, `30×30`, `font-weight:800`, `12.5pt`.
- `.sectionbar`: רקע `--c`, `border-radius:14px`, `page-break-before:always`.
- `.parts`/`.mc`/`.lines`/`table.tbl`/`.note`/`.cover`/`.toc` — כהגדרתם ב-`wsengine.py` (אל תשנו ערכים מקומית).
- איורים: `fig(svg, w=NN)` או `img(name, w=NN)`. `w` = אחוז רוחב מרבי (גרף וקטורי במלוא הרוחב ≈ 160 מ"מ — תמיד מצרים).

## סימון מתמטי (אחיד — חובה)
| מושג | להשתמש | לא להשתמש |
|---|---|---|
| מינוס / שלילי | `−` (U+2212) | `-` (מקף) , `־` (מקף עברי) |
| מעלות | `°` | "deg" |
| זווית | `∠` | `∢` , "זווית X" בנוסחה |
| מקבילים | `∥` | `\|\|` |
| חפיפה | `≅` | `=~` |
| דמיון | `∼` | `~` |
| משולש | `△` | "משולש" בתוך נוסחה קצרה |
| כפל | `×` | `*` (אסטריסק שמור לקוד פייתון בלבד) |
| ≤ / ≥ | `≤` `≥` | `<=` `>=` |
| מפריד בכותרת-משנה | ` · ` (middot) | `-` |
- כל מקטע LTR (נוסחה, שיעורים, משוואה) עטוף ב-`L(...)` כדי למנוע ערבוב כיווניות (bidi).
- תוויות-ציר ב-SVG: מילה בודדת (עברית או לטינית) — לא לערבב לטינית+עברית בסוגריים (נשבר ב-bidi).

## גרפיקה — "החזקה ביותר", בלי דמו
1. גרף/שרטוט שנקבע מתמטית → **שחזור וקטורי**: `charts.coord()`/`grid_of()` (אלגברה) או `geo.py` (גיאומטריה).
2. צילום/גרף-קריאה אמיתי → **חיתוך מדויק מהמקור**, מאומת איור-איור (אסור חיתוך-איחוד-עמוד).
3. אסור איור דמו/ממלא-מקום. אסור להמציא נתונים.
