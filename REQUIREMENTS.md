# דרישות הפרויקט — "התיקייה החכמה" של בעל הפרויקט

> מסמך זה הוא רשומת-האמת של הדרישות הקבועות. **יש לקרוא ולפעול לפיו לפני כל שינוי.**
> מגובה גם בזיכרון הקבוע של Claude (נטען אוטומטית בכל שיחה) → ראו `bbb-math-book`.

## 🧱 הטכנולוגיה שעליה האתר בנוי
- **Next.js 16.2** (App Router, React 19, TypeScript) + **PostgreSQL** (דרך Drizzle ORM).
- ה-Python (`book.py` + `topics/*`) הוא **שלב-בניית-התוכן** שמְזין את Next (מייצר PDF + תמונות-דפים). זה לא "סותר" את הסטאק — זו השכבה שמספקת איכות-דפוס.
- אפליקציית Next ב-`web/` — עצמאית (נתונים ב-Postgres, נכסים ב-`public/worksheets` דרך `npm run content:sync`).

## 🔄 סנכרון — תמיד, בכל שינוי (שלושה יעדים)
1. **מקומי** — השינויים נשמרים במכשיר (`C:\Users\yaniv\bbb_work`).
2. **GitHub** — `push` ל-`origin/main` אחרי כל שיפור (דרך `env -u GH_TOKEN -u GITHUB_TOKEN git push`).
3. **Vercel (אוטומטי)** — לאחר חיבור חד-פעמי של הריפו לחשבון Vercel (Root Directory = `web`), **כל push מפעיל פריסה אוטומטית**. החיבור עצמו דורש את בעל החשבון (אסור ל-Claude ליצור חשבונות/להזין סיסמאות).

## 🔒 כללי-ברזל — אסור לשבור
- **לעולם לא לשנות את התוכן/המלל של דפי-העבודה** — רק עיצוב, מבנה וטכנולוגיה. ה-DB מחזיק מטא-דאטה בלבד.
  - **חריג יחיד (הרשאת הבעלים, 10.7.2026):** עמוד שבור אובייקטיבית — מתמטיקה בלתי-אפשרית,
    איור/נתון חסר שהשאלה מפנה אליו, שאלה בלתי-פתירה — מתקנים באופן עצמאי, מינימלי ונאמן
    לכוונה הדידקטית, ומתעדים בהודעת ה-commit. כל עמוד חייב להיות תקין להדפסה עבור תלמידים.
- **לשמור A4** — כל כרטיס-שאלה ≤ ~260מ"מ (גבול 263). להריץ `python tools/check_uniformity.py --heights` לפני commit.
- **לשמור על אחידות-העיצוב** — Segoe UI, גרדיאנט המותג, כדורי-נושא, צבעי-מקטע, סימון מתמטי תקני (`−` ∠ ∥ △ ≅ ∼ ° · ×). אסור `-` כמינוס, `∢`, `||`, `~`, `*`.
- **רק שיפורים מועילים** — לא להוסיף פיצ'רים שלא מקדמים את הלמידה/השימוש.
- **לא לקלקל מה שקיים** — רק לשפר. אם ניסוי שובר משהו — להחזיר מיד.

## ✍️ כתיב מתמטי — שודרג ל-KaTeX ✅ (29.6.2026)
- **KaTeX** מוטמע בבנייה (מוטמע ב-`vendor/katex/`, committed). עוזר **`M("latex")`** מרנדר
  שברים מוערמים/חזקות/שורשים ברמת ספר-לימוד. כל 14 השברים בספר הומרו ל-`\frac`.
- **להוספת מתמטיקה:** `M("\\frac{a}{b}")` (במקור ה-py כותבים `\\`; `-` ולא `−`;
  `\le`,`\cdot`,`\dots`). אחרי כל הוספה — `python tools/check_uniformity.py --heights`.
- **נשאר Unicode במכוון:** מתמטיקה פשוטה בשורה (y=2x−4, שיעורי-נקודות) וחזקות ²/³ —
  KaTeX מרנדר אותן זהה, אז המרה = סיכון בלי תועלת. **לעולם לא לשנות את משמעות המתמטיקה.**

## 🛠️ שיטת-עבודה
- כל שינוי: **בנייה → אימות (גובה/אחידות/build) → commit + push**. אימות חזותי לעמודים שמושפעים.
- שימוש-חוזר ברכיבי-המנוע: `filltable`, `tg`, `.ebox`, `coord()` (נייר-גרף), `geo.py`.
- Next 16: לקרוא `web/AGENTS.md`; `params` אסינכרוני; `force-dynamic` לדפי-DB; לא להגדיר `turbopack.root`.
